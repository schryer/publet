[pytest]
bdd_features_base_dir = features/
testpaths = .
markers =
    conformance: exercises a normative MUST or MUST NOT of the specification
    plumbing: a single plumbing command
    porcelain: a `pub` subcommand
    scenario: an end-to-end user journey
addopts = --strict-markers -ra
